name: server_example
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.5 <3.0.0'

dependencies:
  args: ^2.0.0
  shelf: ^1.1.0
  shelf_router: ^1.0.0
  
  firedart: ^0.9.0+1
  crypton: ^2.0.5 # elliptic curve keypair + create/verify signature func
  crypto: ^3.0.2 # sha-256
  hex: ^0.2.0 # hex to byte array and vice versa


dev_dependencies:
  http: ^0.13.0
  lints: ^2.0.0
  test: ^1.15.0
